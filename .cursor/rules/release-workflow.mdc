---
description: EyeBreather 项目的发布构建流程
alwaysApply: true
---

# 发布构建流程

本项目使用 GitHub Actions 自动构建和发布 macOS 应用。

## 触发构建

推送以 `v` 开头的 tag 会自动触发构建：

```bash
# 查看现有版本
git tag --list 'v*' | sort -V | tail -5

# 创建新版本 tag（递增版本号）
git tag v1.0.x

# 推送 tag 触发构建
git push origin v1.0.x
```

## 构建产物

- 自动构建 Release 版本的 `.app`
- 打包为 `.dmg` 安装包
- 创建 GitHub Release 并上传 DMG

## 版本号规范

- 格式：`v主版本.次版本.修订号`（如 `v1.0.2`）
- 主版本：重大功能变更
- 次版本：新功能添加
- 修订号：Bug 修复和小改进

## 注意事项

- 普通 push 到 main 分支不会触发构建
- 只有推送 tag 才会触发自动构建和发布
- 查看构建状态：https://github.com/Arkycher/EyeBreather/actions
